[build]
builder = "NIXPACKS"
buildCommand = "yarn install --frozen-lockfile && yarn build:server"

[deploy]
startCommand = "yarn start:server"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
